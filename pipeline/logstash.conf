input {
  beats {
    port => 5044
  }
}

filter {
  # Exemple de filtres (désactivés pour démarrage simple) :
  # grok {
  #   match => { "message" => "%{COMBINEDAPACHELOG}" }
  # }
  # date {
  #   match => ["timestamp", "ISO8601", "yyyy-MM-dd HH:mm:ss"]
  #   target => "@timestamp"
  # }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
